"""core.config — lingzhou.json 加载；`Config` 见 `loader`，子模型见 `core.config_models`。"""
from __future__ import annotations

from .loader import Config, config_reference_defaults

__all__ = ["Config", "config_reference_defaults"]
